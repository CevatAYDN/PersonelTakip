
@{
    ViewBag.Title = "Şifremi Unuttum";
}

<div class="row">
    <div class="col-md-offset-3 col-md-6">
        @using (Html.BeginForm("RecoverPassword", "Acount", FormMethod.Post))
        {
            @Html.AntiForgeryToken()
            @Html.TextBox("email", null, new { @class = "form-control", placeholder = "E-mail Adresi" })<br />
            <input type="submit" value="Sifremi Gönder" class="btn btn-warning" />
        }
    </div>
</div>
